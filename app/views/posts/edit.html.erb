<h1>Editer l'article</h1>

<%= form_for @post do |f| %>

    <div class="form-group">
        <label>Titre de l'article</label>
        <%= f.text_field :name %>
        <%= f.text_area :content %>
    </div>

<% end %>
